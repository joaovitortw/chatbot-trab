# Copie como .env e ajuste conforme necessário.
# Para começar offline/local sem Postgres, use SQLite (default):
DATABASE_URL=sqlite:///./data.db

# Para Postgres (ajuste usuário/senha/host/porta/banco):
# DATABASE_URL=postgresql+psycopg://postgres:senha@localhost:5432/chatbot_trab
# Se usar schema dedicado, você pode setar search_path:
# DATABASE_URL=postgresql+psycopg://app_user:senha@localhost:5432/chatbot_trab?options=-csearch_path=app,public

SESSION_FILE=.session.json
LOG_LEVEL=INFO
